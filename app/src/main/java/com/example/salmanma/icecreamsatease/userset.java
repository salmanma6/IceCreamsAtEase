package com.example.salmanma.icecreamsatease;

import android.support.design.widget.BottomSheetBehavior;
import android.support.design.widget.CollapsingToolbarLayout;
import android.support.design.widget.NavigationView;
import android.support.v4.content.ContextCompat;
import android.support.v4.widget.DrawerLayout;
import android.support.v4.widget.NestedScrollView;
import android.support.v7.app.ActionBarDrawerToggle;
import android.support.v7.app.AppCompatActivity;
import android.os.*;
import android.content.*;
import android.support.v7.widget.SearchView;
import android.support.v7.widget.Toolbar;
import android.view.*;
import android.widget.ImageView;
import android.widget.TextView;

/**
 * Created by Salman M.A on 13-08-2017.
 * Dashboard of user
 */

public class userset extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.cussharebon);
        ImageView t=(ImageView)findViewById(R.id.images);

}}
      /*  fsa=(NestedScrollView)findViewById(R.id.nstscmainindash);
        fsa.setFillViewport(true);
        toolbar = (Toolbar) findViewById(R.id.toolbarindash);
        setSupportActionBar(toolbar);

        CollapsingToolbarLayout cp=(CollapsingToolbarLayout)findViewById(R.id.collaptbdash);
        cp.setContentScrimColor(ContextCompat.getColor(getApplicationContext(),R.color.Menu));
    }
  



}*/
